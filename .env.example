# Copy .env and fill in values. .env is gitignored; credentials live nowhere else.
GCP_PROJECT_ID=
GCP_LOCATION=us-central1
BQ_DATASET=cumberland
GCS_BUCKET=
GEMINI_MODEL=gemini-2.5-flash
PIPELINE_VERSION=0.1.0
